mixin header()
  +b.HEADER.header&attributes(attributes)
    +ui-section-head
      +b.header-content
        +e.H1.__text._top Сервис онлайн-заказ массажа
          br
          | на дом в Москве
        +e.P.__text._bottom Отлично&nbsp;&nbsp;
          +rating(active, 5)
          | &nbsp;рейтинг в AppStore
